# 双扣 - 八王千变 发牌器 - 设计方案

**版本**: v2.0  
**创建时间**: 2026-04-21  
**更新时间**: 2026-04-30  
**作者**: Kami 🐱

---

## 📋 需求分析

### 核心目标
> **保证每个人都有更多的炸弹**

### 游戏规则要点
1. **牌堆**: 2 副牌 (104 张) + 4 大王 + 4 小王 = **112 张**
2. **每人**: 28 张牌
3. **炸弹定义**: 4 张及以上点数相同的牌
4. **特殊牌**: 大王/小王可作为万能牌替代任意牌

---

## 🎯 发牌策略

### 策略核心：连炸预设 → 优先发炸弹 → 补齐手牌

```
步骤 0: 连炸预设（可选，min_chains_per_player > 0 时触发）
       → 13种点数分成4个连续段（3+3+3+4=13），随机偏移
       → 每人分配一个连续段（3~4连环），每种点数取4张
       → 记录已用卡片，剩余4张留给普通炸弹

步骤 1: 统计所有可能的炸弹点数（每种点数 8 张或剩余4张）
       → 共 13 种可形成炸弹的点数 (3~A, 2)
       → 8 张万能牌（4大王 + 4小王）

步骤 2: 轮流给每个玩家分配炸弹
       → 优先给当前炸弹数最少且手牌较少的玩家
       → 确保每人至少 N 个炸弹（默认 2 个）
       → 每个点数的8张牌拆分成两部分给不同玩家

步骤 3: 分配万能牌（八王）
       → 按 jokers_per_player 配置分配

步骤 4: 发完剩余牌
       → 随机洗牌后轮流补齐至 28 张
```

### 炸弹分配逻辑

每个点数有 8 张牌（连炸用过后剩余 4 张），可以：
- **方案 A**: 给 1 个玩家 8 张 → 1 个 8 张炸弹（×16 倍）
- **方案 B**: 给 2 个玩家各 4 张 → 2 个 4 张炸弹（各×1 倍）

**当前实现**: 方案 B 为主（优先保证每人有炸弹）

### 连炸预设逻辑

**触发条件**: `min_chains_per_player > 0`（默认 0 跳过）

**分配方式**:
- 13种点数（3~A, 2）分成 4 个连续段：3+3+3+4=13
- 段大小随机打乱，起始位置随机偏移
- 每人一段，每段内点数相连形成连炸
- 每种点数取 4 张组成炸弹，剩余 4 张留给普通炸弹

**连炸贡献分**:
- 线数 = 最小炸弹张数 + 连环数
- 例：4张×3连环 = 7线 → 4分
- 贡献分 = 2^(线数-5)，4线无分

---

## 📊 测试结果

### 测试配置
- 每人最少炸弹数：2 个
- 连炸预设：关闭（min_chains_per_player=0）
- 测试次数：5000 次

### 测试结果（无连炸）

| 指标 | 数值 |
|------|------|
| **总炸弹数** | 平均 3.38 个/人/局 |
| **4 张炸弹** | ~26% |
| **5 张炸弹** | ~9% |
| **6+ 张炸弹** | ~65% |
| **平均贡献分** | ~2.63 分/人 |

### 测试结果（min_chains_per_player=1）

| 指标 | 数值 |
|------|------|
| **连炸组数** | 4 组/局（每人1组） |
| **连炸大小** | 3~4 连环 |
| **平均贡献分** | ~5.16 分/人（显著提升） |
| **连炸贡献分** | ~4.68 分/人 |
| **单炸贡献分** | ~0.49 分/人 |

---

## 🔧 可配置参数

### 配置文件参数（config.json）

| 参数 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| `min_bombs_per_player` | int | 2 | 每人最少普通炸弹数 |
| `min_chains_per_player` | int | 0 | 每人最少连炸组数（0=跳过） |
| `bomb_size_range` | [int,int] | [4,4] | 预设炸弹大小范围 |
| `jokers_per_player` | int/[int,int] | 2 | 每人万能牌数 |

### 命令行参数

```bash
# 基础用法
python dealer.py

# 指定每人最少炸弹数
python dealer.py --bombs 3

# 指定每人连炸组数
python dealer.py --chains 1

# 指定玩家人数
python dealer.py --players 4

# 固定随机种子（可复现）
python dealer.py --seed 42
```

### 代码级配置

```python
dealer = ShuangkouDealer(num_players=4)

# 完整发牌流程
dealer.deal(
    min_bombs_per_player=2,
    min_chains_per_player=1,  # 每人1组连炸
    bomb_size_range=[4, 6]
)
```

---

## 📈 后续优化方向

### 1. 队伍牌力平衡
- 当前方案可能导致队伍间牌力不均
- 可增加队伍炸弹总数平衡校验

### 2. 炸弹大小分布
- 当前 95% 是 4 张炸弹
- 可增加 5-8 张大炸弹的概率

### 3. 顺子/连对检测
- 检测并避免破坏潜在顺子/连对
- 平衡炸弹和其他牌型

### 4. 连炸组数可配置
- 当前仅支持 0 或 1 组
- 未来可支持每人多组连炸

---

## 🎮 使用场景

### 场景 1: 日常娱乐
```bash
# 每人 2 个炸弹，适度刺激
python dealer.py --bombs 2
```

### 场景 2: 炸弹狂欢
```bash
# 每人 3-4 个炸弹，超级刺激
python dealer.py --bombs 4
```

### 场景 3: 比赛用牌
```bash
# 固定种子，可复现
python dealer.py --bombs 2 --seed 12345
```

### 场景 4: 连炸刺激模式
```bash
# 每人1组连炸 + 3个炸弹
python dealer.py --bombs 3 --chains 1
```

---

## 📁 项目结构

```
shuangkou-dealer/
├── README.md          # 项目说明
├── DESIGN.md          # 设计方案（本文件）
├── RULES.md           # 完整游戏规则
├── FLOWCHART.md       # 发牌流程图
├── dealer.py          # 核心发牌器
├── batch_dealer.py    # 批量模拟 + Excel导出
├── config.json        # 标准配置文件
├── config.template.json # 配置模板
├── test_dealer.py     # 测试套件
└── .git/              # Git 仓库
```

---

## 🐱 设计心得

1. **优先满足核心需求**: 每人更多炸弹是第一优先级
2. **简单即正义**: 先实现基础功能，再迭代优化
3. **测试驱动**: 多项测试确保发牌正确性
4. **可扩展**: 参数化设计，方便后续调整

---

*双扣八王千变 — 炸弹越多越快乐！* 🎉
